{
  "name": "newrelic-azure-log-ingestion",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc && npm run cp:function",
    "build:production": "npm run prestart && npm prune --production",
    "watch": "tsc --w",
    "prestart": "npm run build && func extensions install",
    "start:host": "func start",
    "start": "npm-run-all --parallel start:host watch",
    "test": "echo \"No tests yet...\"",
    "cp:function": "cpx azureLogIngestion/function.json dist/azureLogIngestion"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "@types/node": "^14.14.35",
    "typescript": "^4.0.0",
    "cpx": "^1.5.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@newrelic/telemetry-sdk": "^0.3.0"
  }
}
